#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;

    printf("the area of circule is %f\n", pi + radius);

    // int height = 55;
    // printf("the area of circule is %f", pi * radius * radius * height);
    return 0;
}

